// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"devlog/ent/predicate"
	"devlog/ent/unsavedpost"
	"devlog/ent/unsavedpostimage"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UnsavedPostImageQuery is the builder for querying UnsavedPostImage entities.
type UnsavedPostImageQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.UnsavedPostImage
	// eager-loading edges.
	withUnsavedPost *UnsavedPostQuery
	withFKs         bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UnsavedPostImageQuery builder.
func (upiq *UnsavedPostImageQuery) Where(ps ...predicate.UnsavedPostImage) *UnsavedPostImageQuery {
	upiq.predicates = append(upiq.predicates, ps...)
	return upiq
}

// Limit adds a limit step to the query.
func (upiq *UnsavedPostImageQuery) Limit(limit int) *UnsavedPostImageQuery {
	upiq.limit = &limit
	return upiq
}

// Offset adds an offset step to the query.
func (upiq *UnsavedPostImageQuery) Offset(offset int) *UnsavedPostImageQuery {
	upiq.offset = &offset
	return upiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (upiq *UnsavedPostImageQuery) Unique(unique bool) *UnsavedPostImageQuery {
	upiq.unique = &unique
	return upiq
}

// Order adds an order step to the query.
func (upiq *UnsavedPostImageQuery) Order(o ...OrderFunc) *UnsavedPostImageQuery {
	upiq.order = append(upiq.order, o...)
	return upiq
}

// QueryUnsavedPost chains the current query on the "unsaved_post" edge.
func (upiq *UnsavedPostImageQuery) QueryUnsavedPost() *UnsavedPostQuery {
	query := &UnsavedPostQuery{config: upiq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := upiq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := upiq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(unsavedpostimage.Table, unsavedpostimage.FieldID, selector),
			sqlgraph.To(unsavedpost.Table, unsavedpost.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, unsavedpostimage.UnsavedPostTable, unsavedpostimage.UnsavedPostColumn),
		)
		fromU = sqlgraph.SetNeighbors(upiq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first UnsavedPostImage entity from the query.
// Returns a *NotFoundError when no UnsavedPostImage was found.
func (upiq *UnsavedPostImageQuery) First(ctx context.Context) (*UnsavedPostImage, error) {
	nodes, err := upiq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{unsavedpostimage.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (upiq *UnsavedPostImageQuery) FirstX(ctx context.Context) *UnsavedPostImage {
	node, err := upiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UnsavedPostImage ID from the query.
// Returns a *NotFoundError when no UnsavedPostImage ID was found.
func (upiq *UnsavedPostImageQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = upiq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{unsavedpostimage.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (upiq *UnsavedPostImageQuery) FirstIDX(ctx context.Context) int {
	id, err := upiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UnsavedPostImage entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when exactly one UnsavedPostImage entity is not found.
// Returns a *NotFoundError when no UnsavedPostImage entities are found.
func (upiq *UnsavedPostImageQuery) Only(ctx context.Context) (*UnsavedPostImage, error) {
	nodes, err := upiq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{unsavedpostimage.Label}
	default:
		return nil, &NotSingularError{unsavedpostimage.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (upiq *UnsavedPostImageQuery) OnlyX(ctx context.Context) *UnsavedPostImage {
	node, err := upiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UnsavedPostImage ID in the query.
// Returns a *NotSingularError when exactly one UnsavedPostImage ID is not found.
// Returns a *NotFoundError when no entities are found.
func (upiq *UnsavedPostImageQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = upiq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{unsavedpostimage.Label}
	default:
		err = &NotSingularError{unsavedpostimage.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (upiq *UnsavedPostImageQuery) OnlyIDX(ctx context.Context) int {
	id, err := upiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UnsavedPostImages.
func (upiq *UnsavedPostImageQuery) All(ctx context.Context) ([]*UnsavedPostImage, error) {
	if err := upiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return upiq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (upiq *UnsavedPostImageQuery) AllX(ctx context.Context) []*UnsavedPostImage {
	nodes, err := upiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UnsavedPostImage IDs.
func (upiq *UnsavedPostImageQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := upiq.Select(unsavedpostimage.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (upiq *UnsavedPostImageQuery) IDsX(ctx context.Context) []int {
	ids, err := upiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (upiq *UnsavedPostImageQuery) Count(ctx context.Context) (int, error) {
	if err := upiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return upiq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (upiq *UnsavedPostImageQuery) CountX(ctx context.Context) int {
	count, err := upiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (upiq *UnsavedPostImageQuery) Exist(ctx context.Context) (bool, error) {
	if err := upiq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return upiq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (upiq *UnsavedPostImageQuery) ExistX(ctx context.Context) bool {
	exist, err := upiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UnsavedPostImageQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (upiq *UnsavedPostImageQuery) Clone() *UnsavedPostImageQuery {
	if upiq == nil {
		return nil
	}
	return &UnsavedPostImageQuery{
		config:          upiq.config,
		limit:           upiq.limit,
		offset:          upiq.offset,
		order:           append([]OrderFunc{}, upiq.order...),
		predicates:      append([]predicate.UnsavedPostImage{}, upiq.predicates...),
		withUnsavedPost: upiq.withUnsavedPost.Clone(),
		// clone intermediate query.
		sql:  upiq.sql.Clone(),
		path: upiq.path,
	}
}

// WithUnsavedPost tells the query-builder to eager-load the nodes that are connected to
// the "unsaved_post" edge. The optional arguments are used to configure the query builder of the edge.
func (upiq *UnsavedPostImageQuery) WithUnsavedPost(opts ...func(*UnsavedPostQuery)) *UnsavedPostImageQuery {
	query := &UnsavedPostQuery{config: upiq.config}
	for _, opt := range opts {
		opt(query)
	}
	upiq.withUnsavedPost = query
	return upiq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UUID string `json:"uuid,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UnsavedPostImage.Query().
//		GroupBy(unsavedpostimage.FieldUUID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (upiq *UnsavedPostImageQuery) GroupBy(field string, fields ...string) *UnsavedPostImageGroupBy {
	group := &UnsavedPostImageGroupBy{config: upiq.config}
	group.fields = append([]string{field}, fields...)
	group.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := upiq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return upiq.sqlQuery(ctx), nil
	}
	return group
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UUID string `json:"uuid,omitempty"`
//	}
//
//	client.UnsavedPostImage.Query().
//		Select(unsavedpostimage.FieldUUID).
//		Scan(ctx, &v)
//
func (upiq *UnsavedPostImageQuery) Select(fields ...string) *UnsavedPostImageSelect {
	upiq.fields = append(upiq.fields, fields...)
	return &UnsavedPostImageSelect{UnsavedPostImageQuery: upiq}
}

func (upiq *UnsavedPostImageQuery) prepareQuery(ctx context.Context) error {
	for _, f := range upiq.fields {
		if !unsavedpostimage.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if upiq.path != nil {
		prev, err := upiq.path(ctx)
		if err != nil {
			return err
		}
		upiq.sql = prev
	}
	return nil
}

func (upiq *UnsavedPostImageQuery) sqlAll(ctx context.Context) ([]*UnsavedPostImage, error) {
	var (
		nodes       = []*UnsavedPostImage{}
		withFKs     = upiq.withFKs
		_spec       = upiq.querySpec()
		loadedTypes = [1]bool{
			upiq.withUnsavedPost != nil,
		}
	)
	if upiq.withUnsavedPost != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, unsavedpostimage.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		node := &UnsavedPostImage{config: upiq.config}
		nodes = append(nodes, node)
		return node.scanValues(columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		if len(nodes) == 0 {
			return fmt.Errorf("ent: Assign called without calling ScanValues")
		}
		node := nodes[len(nodes)-1]
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if err := sqlgraph.QueryNodes(ctx, upiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}

	if query := upiq.withUnsavedPost; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*UnsavedPostImage)
		for i := range nodes {
			if nodes[i].unsaved_post_images == nil {
				continue
			}
			fk := *nodes[i].unsaved_post_images
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(unsavedpost.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "unsaved_post_images" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.UnsavedPost = n
			}
		}
	}

	return nodes, nil
}

func (upiq *UnsavedPostImageQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := upiq.querySpec()
	return sqlgraph.CountNodes(ctx, upiq.driver, _spec)
}

func (upiq *UnsavedPostImageQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := upiq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (upiq *UnsavedPostImageQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   unsavedpostimage.Table,
			Columns: unsavedpostimage.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: unsavedpostimage.FieldID,
			},
		},
		From:   upiq.sql,
		Unique: true,
	}
	if unique := upiq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := upiq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, unsavedpostimage.FieldID)
		for i := range fields {
			if fields[i] != unsavedpostimage.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := upiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := upiq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := upiq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := upiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (upiq *UnsavedPostImageQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(upiq.driver.Dialect())
	t1 := builder.Table(unsavedpostimage.Table)
	columns := upiq.fields
	if len(columns) == 0 {
		columns = unsavedpostimage.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if upiq.sql != nil {
		selector = upiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	for _, p := range upiq.predicates {
		p(selector)
	}
	for _, p := range upiq.order {
		p(selector)
	}
	if offset := upiq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := upiq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UnsavedPostImageGroupBy is the group-by builder for UnsavedPostImage entities.
type UnsavedPostImageGroupBy struct {
	config
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (upigb *UnsavedPostImageGroupBy) Aggregate(fns ...AggregateFunc) *UnsavedPostImageGroupBy {
	upigb.fns = append(upigb.fns, fns...)
	return upigb
}

// Scan applies the group-by query and scans the result into the given value.
func (upigb *UnsavedPostImageGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := upigb.path(ctx)
	if err != nil {
		return err
	}
	upigb.sql = query
	return upigb.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (upigb *UnsavedPostImageGroupBy) ScanX(ctx context.Context, v interface{}) {
	if err := upigb.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from group-by.
// It is only allowed when executing a group-by query with one field.
func (upigb *UnsavedPostImageGroupBy) Strings(ctx context.Context) ([]string, error) {
	if len(upigb.fields) > 1 {
		return nil, errors.New("ent: UnsavedPostImageGroupBy.Strings is not achievable when grouping more than 1 field")
	}
	var v []string
	if err := upigb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (upigb *UnsavedPostImageGroupBy) StringsX(ctx context.Context) []string {
	v, err := upigb.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (upigb *UnsavedPostImageGroupBy) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = upigb.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{unsavedpostimage.Label}
	default:
		err = fmt.Errorf("ent: UnsavedPostImageGroupBy.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (upigb *UnsavedPostImageGroupBy) StringX(ctx context.Context) string {
	v, err := upigb.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from group-by.
// It is only allowed when executing a group-by query with one field.
func (upigb *UnsavedPostImageGroupBy) Ints(ctx context.Context) ([]int, error) {
	if len(upigb.fields) > 1 {
		return nil, errors.New("ent: UnsavedPostImageGroupBy.Ints is not achievable when grouping more than 1 field")
	}
	var v []int
	if err := upigb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (upigb *UnsavedPostImageGroupBy) IntsX(ctx context.Context) []int {
	v, err := upigb.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (upigb *UnsavedPostImageGroupBy) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = upigb.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{unsavedpostimage.Label}
	default:
		err = fmt.Errorf("ent: UnsavedPostImageGroupBy.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (upigb *UnsavedPostImageGroupBy) IntX(ctx context.Context) int {
	v, err := upigb.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from group-by.
// It is only allowed when executing a group-by query with one field.
func (upigb *UnsavedPostImageGroupBy) Float64s(ctx context.Context) ([]float64, error) {
	if len(upigb.fields) > 1 {
		return nil, errors.New("ent: UnsavedPostImageGroupBy.Float64s is not achievable when grouping more than 1 field")
	}
	var v []float64
	if err := upigb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (upigb *UnsavedPostImageGroupBy) Float64sX(ctx context.Context) []float64 {
	v, err := upigb.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (upigb *UnsavedPostImageGroupBy) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = upigb.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{unsavedpostimage.Label}
	default:
		err = fmt.Errorf("ent: UnsavedPostImageGroupBy.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (upigb *UnsavedPostImageGroupBy) Float64X(ctx context.Context) float64 {
	v, err := upigb.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from group-by.
// It is only allowed when executing a group-by query with one field.
func (upigb *UnsavedPostImageGroupBy) Bools(ctx context.Context) ([]bool, error) {
	if len(upigb.fields) > 1 {
		return nil, errors.New("ent: UnsavedPostImageGroupBy.Bools is not achievable when grouping more than 1 field")
	}
	var v []bool
	if err := upigb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (upigb *UnsavedPostImageGroupBy) BoolsX(ctx context.Context) []bool {
	v, err := upigb.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (upigb *UnsavedPostImageGroupBy) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = upigb.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{unsavedpostimage.Label}
	default:
		err = fmt.Errorf("ent: UnsavedPostImageGroupBy.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (upigb *UnsavedPostImageGroupBy) BoolX(ctx context.Context) bool {
	v, err := upigb.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (upigb *UnsavedPostImageGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range upigb.fields {
		if !unsavedpostimage.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := upigb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := upigb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (upigb *UnsavedPostImageGroupBy) sqlQuery() *sql.Selector {
	selector := upigb.sql.Select()
	aggregation := make([]string, 0, len(upigb.fns))
	for _, fn := range upigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(upigb.fields)+len(upigb.fns))
		for _, f := range upigb.fields {
			columns = append(columns, selector.C(f))
		}
		for _, c := range aggregation {
			columns = append(columns, c)
		}
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(upigb.fields...)...)
}

// UnsavedPostImageSelect is the builder for selecting fields of UnsavedPostImage entities.
type UnsavedPostImageSelect struct {
	*UnsavedPostImageQuery
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (upis *UnsavedPostImageSelect) Scan(ctx context.Context, v interface{}) error {
	if err := upis.prepareQuery(ctx); err != nil {
		return err
	}
	upis.sql = upis.UnsavedPostImageQuery.sqlQuery(ctx)
	return upis.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (upis *UnsavedPostImageSelect) ScanX(ctx context.Context, v interface{}) {
	if err := upis.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from a selector. It is only allowed when selecting one field.
func (upis *UnsavedPostImageSelect) Strings(ctx context.Context) ([]string, error) {
	if len(upis.fields) > 1 {
		return nil, errors.New("ent: UnsavedPostImageSelect.Strings is not achievable when selecting more than 1 field")
	}
	var v []string
	if err := upis.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (upis *UnsavedPostImageSelect) StringsX(ctx context.Context) []string {
	v, err := upis.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a selector. It is only allowed when selecting one field.
func (upis *UnsavedPostImageSelect) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = upis.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{unsavedpostimage.Label}
	default:
		err = fmt.Errorf("ent: UnsavedPostImageSelect.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (upis *UnsavedPostImageSelect) StringX(ctx context.Context) string {
	v, err := upis.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from a selector. It is only allowed when selecting one field.
func (upis *UnsavedPostImageSelect) Ints(ctx context.Context) ([]int, error) {
	if len(upis.fields) > 1 {
		return nil, errors.New("ent: UnsavedPostImageSelect.Ints is not achievable when selecting more than 1 field")
	}
	var v []int
	if err := upis.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (upis *UnsavedPostImageSelect) IntsX(ctx context.Context) []int {
	v, err := upis.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a selector. It is only allowed when selecting one field.
func (upis *UnsavedPostImageSelect) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = upis.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{unsavedpostimage.Label}
	default:
		err = fmt.Errorf("ent: UnsavedPostImageSelect.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (upis *UnsavedPostImageSelect) IntX(ctx context.Context) int {
	v, err := upis.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from a selector. It is only allowed when selecting one field.
func (upis *UnsavedPostImageSelect) Float64s(ctx context.Context) ([]float64, error) {
	if len(upis.fields) > 1 {
		return nil, errors.New("ent: UnsavedPostImageSelect.Float64s is not achievable when selecting more than 1 field")
	}
	var v []float64
	if err := upis.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (upis *UnsavedPostImageSelect) Float64sX(ctx context.Context) []float64 {
	v, err := upis.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a selector. It is only allowed when selecting one field.
func (upis *UnsavedPostImageSelect) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = upis.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{unsavedpostimage.Label}
	default:
		err = fmt.Errorf("ent: UnsavedPostImageSelect.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (upis *UnsavedPostImageSelect) Float64X(ctx context.Context) float64 {
	v, err := upis.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from a selector. It is only allowed when selecting one field.
func (upis *UnsavedPostImageSelect) Bools(ctx context.Context) ([]bool, error) {
	if len(upis.fields) > 1 {
		return nil, errors.New("ent: UnsavedPostImageSelect.Bools is not achievable when selecting more than 1 field")
	}
	var v []bool
	if err := upis.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (upis *UnsavedPostImageSelect) BoolsX(ctx context.Context) []bool {
	v, err := upis.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a selector. It is only allowed when selecting one field.
func (upis *UnsavedPostImageSelect) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = upis.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{unsavedpostimage.Label}
	default:
		err = fmt.Errorf("ent: UnsavedPostImageSelect.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (upis *UnsavedPostImageSelect) BoolX(ctx context.Context) bool {
	v, err := upis.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (upis *UnsavedPostImageSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := upis.sql.Query()
	if err := upis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
