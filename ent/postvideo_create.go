// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"devlog/ent/post"
	"devlog/ent/postvideo"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PostVideoCreate is the builder for creating a PostVideo entity.
type PostVideoCreate struct {
	config
	mutation *PostVideoMutation
	hooks    []Hook
}

// SetUUID sets the "uuid" field.
func (pvc *PostVideoCreate) SetUUID(s string) *PostVideoCreate {
	pvc.mutation.SetUUID(s)
	return pvc
}

// SetTitle sets the "title" field.
func (pvc *PostVideoCreate) SetTitle(s string) *PostVideoCreate {
	pvc.mutation.SetTitle(s)
	return pvc
}

// SetURL sets the "url" field.
func (pvc *PostVideoCreate) SetURL(s string) *PostVideoCreate {
	pvc.mutation.SetURL(s)
	return pvc
}

// SetCreatedAt sets the "created_at" field.
func (pvc *PostVideoCreate) SetCreatedAt(t time.Time) *PostVideoCreate {
	pvc.mutation.SetCreatedAt(t)
	return pvc
}

// SetNillableCreatedAt sets the "created_at" field if the given value is not nil.
func (pvc *PostVideoCreate) SetNillableCreatedAt(t *time.Time) *PostVideoCreate {
	if t != nil {
		pvc.SetCreatedAt(*t)
	}
	return pvc
}

// SetPostID sets the "post" edge to the Post entity by ID.
func (pvc *PostVideoCreate) SetPostID(id int) *PostVideoCreate {
	pvc.mutation.SetPostID(id)
	return pvc
}

// SetPost sets the "post" edge to the Post entity.
func (pvc *PostVideoCreate) SetPost(p *Post) *PostVideoCreate {
	return pvc.SetPostID(p.ID)
}

// Mutation returns the PostVideoMutation object of the builder.
func (pvc *PostVideoCreate) Mutation() *PostVideoMutation {
	return pvc.mutation
}

// Save creates the PostVideo in the database.
func (pvc *PostVideoCreate) Save(ctx context.Context) (*PostVideo, error) {
	var (
		err  error
		node *PostVideo
	)
	pvc.defaults()
	if len(pvc.hooks) == 0 {
		if err = pvc.check(); err != nil {
			return nil, err
		}
		node, err = pvc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*PostVideoMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = pvc.check(); err != nil {
				return nil, err
			}
			pvc.mutation = mutation
			if node, err = pvc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(pvc.hooks) - 1; i >= 0; i-- {
			if pvc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = pvc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, pvc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (pvc *PostVideoCreate) SaveX(ctx context.Context) *PostVideo {
	v, err := pvc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pvc *PostVideoCreate) Exec(ctx context.Context) error {
	_, err := pvc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pvc *PostVideoCreate) ExecX(ctx context.Context) {
	if err := pvc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (pvc *PostVideoCreate) defaults() {
	if _, ok := pvc.mutation.CreatedAt(); !ok {
		v := postvideo.DefaultCreatedAt()
		pvc.mutation.SetCreatedAt(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pvc *PostVideoCreate) check() error {
	if _, ok := pvc.mutation.UUID(); !ok {
		return &ValidationError{Name: "uuid", err: errors.New(`ent: missing required field "uuid"`)}
	}
	if v, ok := pvc.mutation.UUID(); ok {
		if err := postvideo.UUIDValidator(v); err != nil {
			return &ValidationError{Name: "uuid", err: fmt.Errorf(`ent: validator failed for field "uuid": %w`, err)}
		}
	}
	if _, ok := pvc.mutation.Title(); !ok {
		return &ValidationError{Name: "title", err: errors.New(`ent: missing required field "title"`)}
	}
	if v, ok := pvc.mutation.Title(); ok {
		if err := postvideo.TitleValidator(v); err != nil {
			return &ValidationError{Name: "title", err: fmt.Errorf(`ent: validator failed for field "title": %w`, err)}
		}
	}
	if _, ok := pvc.mutation.URL(); !ok {
		return &ValidationError{Name: "url", err: errors.New(`ent: missing required field "url"`)}
	}
	if v, ok := pvc.mutation.URL(); ok {
		if err := postvideo.URLValidator(v); err != nil {
			return &ValidationError{Name: "url", err: fmt.Errorf(`ent: validator failed for field "url": %w`, err)}
		}
	}
	if _, ok := pvc.mutation.CreatedAt(); !ok {
		return &ValidationError{Name: "created_at", err: errors.New(`ent: missing required field "created_at"`)}
	}
	if _, ok := pvc.mutation.PostID(); !ok {
		return &ValidationError{Name: "post", err: errors.New("ent: missing required edge \"post\"")}
	}
	return nil
}

func (pvc *PostVideoCreate) sqlSave(ctx context.Context) (*PostVideo, error) {
	_node, _spec := pvc.createSpec()
	if err := sqlgraph.CreateNode(ctx, pvc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (pvc *PostVideoCreate) createSpec() (*PostVideo, *sqlgraph.CreateSpec) {
	var (
		_node = &PostVideo{config: pvc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: postvideo.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: postvideo.FieldID,
			},
		}
	)
	if value, ok := pvc.mutation.UUID(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: postvideo.FieldUUID,
		})
		_node.UUID = value
	}
	if value, ok := pvc.mutation.Title(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: postvideo.FieldTitle,
		})
		_node.Title = value
	}
	if value, ok := pvc.mutation.URL(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: postvideo.FieldURL,
		})
		_node.URL = value
	}
	if value, ok := pvc.mutation.CreatedAt(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: postvideo.FieldCreatedAt,
		})
		_node.CreatedAt = value
	}
	if nodes := pvc.mutation.PostIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   postvideo.PostTable,
			Columns: []string{postvideo.PostColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: post.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.post_videos = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// PostVideoCreateBulk is the builder for creating many PostVideo entities in bulk.
type PostVideoCreateBulk struct {
	config
	builders []*PostVideoCreate
}

// Save creates the PostVideo entities in the database.
func (pvcb *PostVideoCreateBulk) Save(ctx context.Context) ([]*PostVideo, error) {
	specs := make([]*sqlgraph.CreateSpec, len(pvcb.builders))
	nodes := make([]*PostVideo, len(pvcb.builders))
	mutators := make([]Mutator, len(pvcb.builders))
	for i := range pvcb.builders {
		func(i int, root context.Context) {
			builder := pvcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*PostVideoMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pvcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pvcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pvcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pvcb *PostVideoCreateBulk) SaveX(ctx context.Context) []*PostVideo {
	v, err := pvcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pvcb *PostVideoCreateBulk) Exec(ctx context.Context) error {
	_, err := pvcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pvcb *PostVideoCreateBulk) ExecX(ctx context.Context) {
	if err := pvcb.Exec(ctx); err != nil {
		panic(err)
	}
}
