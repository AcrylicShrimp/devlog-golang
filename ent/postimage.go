// Code generated by entc, DO NOT EDIT.

package ent

import (
	"devlog/ent/post"
	"devlog/ent/postimage"
	"fmt"
	"strings"
	"time"

	"github.com/facebook/ent/dialect/sql"
)

// PostImage is the model entity for the PostImage schema.
type PostImage struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// UUID holds the value of the "uuid" field.
	UUID string `json:"uuid,omitempty"`
	// Width holds the value of the "width" field.
	Width uint32 `json:"width,omitempty"`
	// Height holds the value of the "height" field.
	Height uint32 `json:"height,omitempty"`
	// Hash holds the value of the "hash" field.
	Hash string `json:"hash,omitempty"`
	// Title holds the value of the "title" field.
	Title string `json:"title,omitempty"`
	// URL holds the value of the "url" field.
	URL string `json:"url,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PostImageQuery when eager-loading is set.
	Edges       PostImageEdges `json:"edges"`
	post_images *int
}

// PostImageEdges holds the relations/edges for other nodes in the graph.
type PostImageEdges struct {
	// Post holds the value of the post edge.
	Post *Post
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// PostOrErr returns the Post value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PostImageEdges) PostOrErr() (*Post, error) {
	if e.loadedTypes[0] {
		if e.Post == nil {
			// The edge post was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: post.Label}
		}
		return e.Post, nil
	}
	return nil, &NotLoadedError{edge: "post"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PostImage) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case postimage.FieldID, postimage.FieldWidth, postimage.FieldHeight:
			values[i] = &sql.NullInt64{}
		case postimage.FieldUUID, postimage.FieldHash, postimage.FieldTitle, postimage.FieldURL:
			values[i] = &sql.NullString{}
		case postimage.FieldCreatedAt:
			values[i] = &sql.NullTime{}
		case postimage.ForeignKeys[0]: // post_images
			values[i] = &sql.NullInt64{}
		default:
			return nil, fmt.Errorf("unexpected column %q for type PostImage", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PostImage fields.
func (pi *PostImage) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case postimage.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			pi.ID = int(value.Int64)
		case postimage.FieldUUID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field uuid", values[i])
			} else if value.Valid {
				pi.UUID = value.String
			}
		case postimage.FieldWidth:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field width", values[i])
			} else if value.Valid {
				pi.Width = uint32(value.Int64)
			}
		case postimage.FieldHeight:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field height", values[i])
			} else if value.Valid {
				pi.Height = uint32(value.Int64)
			}
		case postimage.FieldHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field hash", values[i])
			} else if value.Valid {
				pi.Hash = value.String
			}
		case postimage.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				pi.Title = value.String
			}
		case postimage.FieldURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field url", values[i])
			} else if value.Valid {
				pi.URL = value.String
			}
		case postimage.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				pi.CreatedAt = value.Time
			}
		case postimage.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field post_images", value)
			} else if value.Valid {
				pi.post_images = new(int)
				*pi.post_images = int(value.Int64)
			}
		}
	}
	return nil
}

// QueryPost queries the "post" edge of the PostImage entity.
func (pi *PostImage) QueryPost() *PostQuery {
	return (&PostImageClient{config: pi.config}).QueryPost(pi)
}

// Update returns a builder for updating this PostImage.
// Note that you need to call PostImage.Unwrap() before calling this method if this PostImage
// was returned from a transaction, and the transaction was committed or rolled back.
func (pi *PostImage) Update() *PostImageUpdateOne {
	return (&PostImageClient{config: pi.config}).UpdateOne(pi)
}

// Unwrap unwraps the PostImage entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pi *PostImage) Unwrap() *PostImage {
	tx, ok := pi.config.driver.(*txDriver)
	if !ok {
		panic("ent: PostImage is not a transactional entity")
	}
	pi.config.driver = tx.drv
	return pi
}

// String implements the fmt.Stringer.
func (pi *PostImage) String() string {
	var builder strings.Builder
	builder.WriteString("PostImage(")
	builder.WriteString(fmt.Sprintf("id=%v", pi.ID))
	builder.WriteString(", uuid=")
	builder.WriteString(pi.UUID)
	builder.WriteString(", width=")
	builder.WriteString(fmt.Sprintf("%v", pi.Width))
	builder.WriteString(", height=")
	builder.WriteString(fmt.Sprintf("%v", pi.Height))
	builder.WriteString(", hash=")
	builder.WriteString(pi.Hash)
	builder.WriteString(", title=")
	builder.WriteString(pi.Title)
	builder.WriteString(", url=")
	builder.WriteString(pi.URL)
	builder.WriteString(", created_at=")
	builder.WriteString(pi.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// PostImages is a parsable slice of PostImage.
type PostImages []*PostImage

func (pi PostImages) config(cfg config) {
	for _i := range pi {
		pi[_i].config = cfg
	}
}
