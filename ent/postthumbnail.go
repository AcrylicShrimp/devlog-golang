// Code generated by entc, DO NOT EDIT.

package ent

import (
	"devlog/ent/post"
	"devlog/ent/postthumbnail"
	"fmt"
	"strings"
	"time"

	"github.com/facebook/ent/dialect/sql"
)

// PostThumbnail is the model entity for the PostThumbnail schema.
type PostThumbnail struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Width holds the value of the "width" field.
	Width uint32 `json:"width,omitempty"`
	// Height holds the value of the "height" field.
	Height uint32 `json:"height,omitempty"`
	// Hash holds the value of the "hash" field.
	Hash string `json:"hash,omitempty"`
	// URL holds the value of the "url" field.
	URL string `json:"url,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PostThumbnailQuery when eager-loading is set.
	Edges          PostThumbnailEdges `json:"edges"`
	post_thumbnail *int
}

// PostThumbnailEdges holds the relations/edges for other nodes in the graph.
type PostThumbnailEdges struct {
	// Post holds the value of the post edge.
	Post *Post
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// PostOrErr returns the Post value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PostThumbnailEdges) PostOrErr() (*Post, error) {
	if e.loadedTypes[0] {
		if e.Post == nil {
			// The edge post was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: post.Label}
		}
		return e.Post, nil
	}
	return nil, &NotLoadedError{edge: "post"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PostThumbnail) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case postthumbnail.FieldID, postthumbnail.FieldWidth, postthumbnail.FieldHeight:
			values[i] = &sql.NullInt64{}
		case postthumbnail.FieldHash, postthumbnail.FieldURL:
			values[i] = &sql.NullString{}
		case postthumbnail.FieldCreatedAt:
			values[i] = &sql.NullTime{}
		case postthumbnail.ForeignKeys[0]: // post_thumbnail
			values[i] = &sql.NullInt64{}
		default:
			return nil, fmt.Errorf("unexpected column %q for type PostThumbnail", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PostThumbnail fields.
func (pt *PostThumbnail) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case postthumbnail.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			pt.ID = int(value.Int64)
		case postthumbnail.FieldWidth:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field width", values[i])
			} else if value.Valid {
				pt.Width = uint32(value.Int64)
			}
		case postthumbnail.FieldHeight:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field height", values[i])
			} else if value.Valid {
				pt.Height = uint32(value.Int64)
			}
		case postthumbnail.FieldHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field hash", values[i])
			} else if value.Valid {
				pt.Hash = value.String
			}
		case postthumbnail.FieldURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field url", values[i])
			} else if value.Valid {
				pt.URL = value.String
			}
		case postthumbnail.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				pt.CreatedAt = value.Time
			}
		case postthumbnail.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field post_thumbnail", value)
			} else if value.Valid {
				pt.post_thumbnail = new(int)
				*pt.post_thumbnail = int(value.Int64)
			}
		}
	}
	return nil
}

// QueryPost queries the "post" edge of the PostThumbnail entity.
func (pt *PostThumbnail) QueryPost() *PostQuery {
	return (&PostThumbnailClient{config: pt.config}).QueryPost(pt)
}

// Update returns a builder for updating this PostThumbnail.
// Note that you need to call PostThumbnail.Unwrap() before calling this method if this PostThumbnail
// was returned from a transaction, and the transaction was committed or rolled back.
func (pt *PostThumbnail) Update() *PostThumbnailUpdateOne {
	return (&PostThumbnailClient{config: pt.config}).UpdateOne(pt)
}

// Unwrap unwraps the PostThumbnail entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pt *PostThumbnail) Unwrap() *PostThumbnail {
	tx, ok := pt.config.driver.(*txDriver)
	if !ok {
		panic("ent: PostThumbnail is not a transactional entity")
	}
	pt.config.driver = tx.drv
	return pt
}

// String implements the fmt.Stringer.
func (pt *PostThumbnail) String() string {
	var builder strings.Builder
	builder.WriteString("PostThumbnail(")
	builder.WriteString(fmt.Sprintf("id=%v", pt.ID))
	builder.WriteString(", width=")
	builder.WriteString(fmt.Sprintf("%v", pt.Width))
	builder.WriteString(", height=")
	builder.WriteString(fmt.Sprintf("%v", pt.Height))
	builder.WriteString(", hash=")
	builder.WriteString(pt.Hash)
	builder.WriteString(", url=")
	builder.WriteString(pt.URL)
	builder.WriteString(", created_at=")
	builder.WriteString(pt.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// PostThumbnails is a parsable slice of PostThumbnail.
type PostThumbnails []*PostThumbnail

func (pt PostThumbnails) config(cfg config) {
	for _i := range pt {
		pt[_i].config = cfg
	}
}
